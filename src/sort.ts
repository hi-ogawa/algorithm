// what i need
// write test by jasmine with coffeescript
// watch file updates and compile and run test automatically

module Sort {

    export function insertSort (arr: number[]) : (number[]) {
        return arr;
    }

}